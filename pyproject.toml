[project]
name = "excel-image-matcher-cli"
version = "0.1.0"
description = "Public-safe spreadsheet image matching CLI with synthetic data"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
